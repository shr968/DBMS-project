<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Patient Dashboard</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f0f8ff; /* Light Blue Background */
            text-align: center;
            color: #000; /* Black Text */
            padding: 30px;
        }
        h1 {
            color: #007bff; /* Bright Blue */
        }
        .container {
            width: 80%;
            margin: auto;
        }
        .card {
            display: inline-block;
            width: 220px;
            height: 150px;
            margin: 15px;
            padding: 20px;
            border: 2px solid #007bff; /* Blue Border */
            border-radius: 10px;
            text-align: center;
            font-size: 18px;
            cursor: pointer;
            background-color: #ffffff; /* White Background */
            color: #007bff; /* Blue Text */
            font-weight: bold;
            transition: 0.3s;
            box-shadow: 0px 4px 6px rgba(0, 0, 0, 0.1);
        }
        .card:hover {
            background-color: #e0ebff; /* Light Blue Hover */
        }
    </style>
</head>
<body>
    <h1>Welcome, Patient <%= patient_id %></h1>
    <div class="container">
        <div class="card" onclick="location.href='/records/<%= patient_id %>'">Records</div>
        <div class="card" onclick="location.href='/prescriptions/<%= patient_id %>'">Prescriptions</div>
        <div class="card" onclick="location.href='/insurance/<%= patient_id %>'">Insurance</div>
        <div class="card" onclick="location.href='/emergency/<%= patient_id %>'">Emergency Contacts</div>
        <div class="card" onclick="location.href='/treatment/<%= patient_id %>'">Current Treatment</div> 
    </div>
</body>
</html>
