<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Hospital Dashboard</title>
    <style>
        @import url("https://fonts.googleapis.com/css2?family=Poppins:wght@100;200;300;400;500;600;700;800;900&display=swap");

        :root {
            --blue: #00b8b8;
            --black: #333;
            --white: #fff;
            --light-bg: #eee;
            --border: 0.2rem solid rgba(0, 0, 0, 0.1);
            --box-shadow: 0 0.5rem 1rem rgba(0, 0, 0, 0.1);
        }

        * {
            font-family: "Poppins", sans-serif;
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        html, body {
            background: var(--light-bg);
            font-size: 62.5%;
        }

        .header-banner {
            background: var(--blue);
            color: var(--white);
            text-align: center;
            padding: 1rem;
            width: 100%;
            position: fixed;
            top: 0;
            left: 0;
            right: 0;
        }

        .header-banner h1 {
            font-size: 2.5rem;
            font-weight: 600;
        }

        .container {
            width: 70%;
            margin: 6rem auto 2rem;
            background: var(--white);
            padding: 2rem;
            border-radius: 10px;
            box-shadow: var(--box-shadow);
            border: var(--border);
        }

        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 1.5rem;
            background: var(--white);
            border-radius: 8px;
        }

        th, td {
            border: var(--border);
            padding: 8px;
            text-align: left;
            font-size: 1.4rem;
        }

        th {
            background: var(--blue);
            color: var(--white);
        }

        tr:nth-child(even) {
            background: var(--light-bg);
        }

        a {
            text-decoration: none;
            color: var(--blue);
            font-weight: bold;
        }

        a:hover {
            text-decoration: underline;
        }
    </style>
</head>
<body>
    <div class="header-banner">
        <h1>Welcome, Hospital <%= hospital_id %></h1>
    </div>

    <div class="container">
        <center><h1 class="heading" style="font-size: 2rem;">PATIENTS BEING TREATED</h1></center>
        <h2 style="font-size: 1.6rem; margin-bottom: 1rem;">
            Total Patients Being Treated: <span style="color: var(--blue);"><%= total_patients %></span>
        </h2>        
        <table>
            <tr>
                <th>Patient ID</th>
                <th>Patient Name</th>
                <th>Doctor Name</th>
                <th>View Dashboard</th>
            </tr>
            <% treatments.forEach(treatment => { %>
            <tr>
                <td><%= treatment.patient_id %></td>
                <td><%= treatment.first_name %> <%= treatment.last_name %></td>
                <td><%= treatment.doctor_name %></td>
                <td><a href="/patient-dashboard/<%= treatment.patient_id %>">View Dashboard</a></td>
            </tr>
            <% }) %>
        </table>
    </div>    
</body>
</html>