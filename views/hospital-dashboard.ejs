<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Hospital Dashboard</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f0f8ff; /* Light Blue */
            color: #000; /* Black Text */
            text-align: center;
            padding: 20px;
        }
        h1 {
            color: #007bff; /* Blue */
        }
        .container {
            width: 70%;
            margin: auto;
            background-color: #fff; /* White Background */
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0px 4px 6px rgba(0, 0, 0, 0.1);
            border: 2px solid #007bff;
        }
        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 20px;
            background-color: #ffffff; /* White Table */
            border-radius: 8px;
        }
        th, td {
            border: 1px solid #ccc;
            padding: 12px;
            text-align: left;
        }
        th {
            background-color: #007bff; /* Blue Header */
            color: white;
        }
        tr:nth-child(even) {
            background-color: #f2f2f2; /* Light Grey for Alternate Rows */
        }
        a {
            text-decoration: none;
            color: #007bff;
            font-weight: bold;
        }
        a:hover {
            text-decoration: underline;
        }
    </style>
</head>
<body>
    <h1>Welcome, Hospital <%= hospital_id %></h1>

    <div class="container">
        <h2>Patients Being Treated</h2>
        <table>
            <tr>
                <th>Patient ID</th>
                <th>Patient Name</th>
                <th>Doctor Name</th>
                <th>View Dashboard</th>
            </tr>
            <% treatments.forEach(treatment => { %>
            <tr>
                <td><%= treatment.patient_id %></td>
                <td><%= treatment.first_name %> <%= treatment.last_name %></td>
                <td><%= treatment.doctor_name %></td>
                <td><a href="/patient-dashboard/<%= treatment.patient_id %>">View Dashboard</a></td>
            </tr>
            <% }) %>
        </table>
    </div>
</body>
</html>
